#include <iostream>
using namespace std;

int s(int mas[], const int i);

int main(){
const int i = 25;
int p[i] = {15, .. , 94 };
for (int i=0; i<l; i++){
    cout << "p[" << i << "] = ";
    cin >> p[i];
}

for (int i=0; i<l; i++)
cout << p[i] << " ";
cout << endl;
return 0;

int s(int mas[], const int i){
    int s=0;
    for (int i=0; i<l; i++);
    s += mas[i]
}

if (!(p[i]%2=0 && i%13=0)){
    s += p[i];
    k++;
    p[i]=0;
}
return s;
}
